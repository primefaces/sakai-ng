import {Timing} from "../timing";
import {RoomTable} from "../room-table";

export interface EventInterface {
  id: string;
    title: string;
    start: string;
    end: string;
    color: string;
}
